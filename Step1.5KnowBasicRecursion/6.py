# Revese an array using recursion
def reverseList(listOfNum, Size):
    if Size % 2 == 0:
        



listOfNum = [1,2,3,4,5]
Size = len(listOfNum)
print("Reverse of array is : ", reverseList(listOfNum, Size))